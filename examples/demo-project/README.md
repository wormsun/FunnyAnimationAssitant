# Demo Project

这是一个最小 `.anime` 示例项目，用来验证本地项目打开流程和项目文件格式。

使用方式：

1. 在仓库根目录运行 `npm run dev`。
2. 打开浏览器中的本地开发地址。
3. 点击“打开项目文件夹”。
4. 选择 `examples/demo-project` 文件夹。
5. 在文件选择弹窗中打开 `demo.anime`。

该示例不包含外部图片、音频或字体素材，因此适合放入公开仓库。你可以在打开后自行添加角色、背景、道具、剧本和音频。
